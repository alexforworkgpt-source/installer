---
type: issue
title: Безопасное обновление Bot Stack с защитой PostgreSQL
execution: AFK
status: done
labels:
  - done
created: 2026-08-14
---

# Безопасное обновление Bot Stack с защитой PostgreSQL

## Parent

[PRD: Безопасная и воспроизводимая установка Bedolaga Bot Stack](./prd.md)

## What to build

Провести compatible Release Bundle через Runtime Change Transaction с защитой пользовательских данных. Перед потенциально schema-changing Bot update installer должен создать и проверить PostgreSQL dump, зафиксировать текущую Alembic revision и применить заявленную release migration policy. Успех означает совместно обновлённые и проверенные Bot, Cabinet, database schema, Caddy и Telegram routing.

Если release допускает rollback, installer возвращает точный предыдущий bundle и проверяет его. Если database migration необратима для предыдущего Bot, installer не имитирует rollback, а переводит стек в безопасное состояние и выдаёт точный recovery plan.

## Acceptance criteria

- [x] Update принимает только разрешённый и совместимый Release Bundle.
- [x] До изменения Bot создаётся читаемый проверенный PostgreSQL dump.
- [x] До и после update записываются фактические Alembic revisions.
- [x] Migration compatibility из release metadata определяет допустимую rollback policy.
- [x] Bot, Cabinet, images и generated runtime применяются как одна транзакционная операция.
- [x] Успех фиксируется только после database, Bot, Cabinet, Caddy и Telegram verification.
- [x] Совместимый rollback использует точный предыдущий Release Bundle и проходит post-rollback health.
- [x] Несовместимая database migration приводит к safely stopped outcome с recovery plan, а не к ложному сообщению об успешном rollback.
- [x] Telegram pending updates не удаляются по умолчанию во время update.
- [x] Failure-injection tests покрывают checkout, Cabinet activation, migration, Compose, Caddy, Telegram и final health stages.
- [x] Update documentation объясняет backup, migration policy, rollback и safely stopped outcome.

## Verification record

> *This was generated by AI during triage.*

2026-08-15 полный disposable Ubuntu 24.04 lifecycle применил публичный Bundle
`2026.08.1`, создал и проверил PostgreSQL dump, зафиксировал Alembic `0104` до и
после update и получил `committed`. Повторный update был намеренно остановлен на
`verify-release` после runtime mutation; точный предыдущий Bundle и dump были
восстановлены, result — `rolled_back`, `rollback_verified=true`, итоговый doctor
прошёл. Локальные failure-injection harnesses покрывают также `forward-only`
`safely_stopped` и все перечисленные stages.

## Test server authorization

> *This was generated by AI during triage.*

2026-08-15 пользователь явно подтвердил, что текущий сервер является тестовым и
одноразовым. Для end-to-end install/update/rollback proof на этом сервере разрешены
destructive operations; при запуске gate следует использовать
`SERVER_IS_DISPOSABLE=yes`.

Разрешение относится к текущему тестовому серверу и не должно автоматически
переноситься на другие VPS или production-среды.

## Blocked by

- [Issue 03: Детерминированный Release Bundle и Cabinet artifact](./issue-03-deterministic-release-bundle.md)
- [Issue 04: Runtime Change Transaction для установки и настроек](./issue-04-runtime-change-transaction.md)
