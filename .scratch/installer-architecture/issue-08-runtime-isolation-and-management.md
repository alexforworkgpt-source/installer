---
type: issue
title: Изоляция runtime и стабильное управление installer
execution: AFK
status: done
labels:
  - done
created: 2026-08-14
---

# Изоляция runtime и стабильное управление installer

## Parent

[PRD: Безопасная и воспроизводимая установка Bedolaga Bot Stack](./prd.md)

## What to build

Уменьшить privileges работающего стека и отвязать управление от случайного исходного clone installer. Bot должен работать как non-root application user с корректно подготовленными persistent directories. Compose resources должны изолироваться стабильным project identity без глобальных container names. Management launcher должен указывать на установленную версионированную копию installer и сохраняться при обычном удалении Bot Stack.

Срез должен доказать, что production и изолированный staging project могут сосуществовать при разных domains и host ports, а uninstall удаляет только выбранный stack и не уничтожает инструмент восстановления без отдельного подтверждения.

## Acceptance criteria

- [x] Bot container работает как non-root application user.
- [x] Data, logs, uploads и backups получают минимально необходимые ownership и permissions без запуска Bot как root.
- [x] Compose использует стабильный project identity и не требует global container names.
- [x] Два изолированных projects могут пройти Compose validation и запуск без resource-name collisions при разных host ports и domains.
- [x] Backend остаётся привязанным к loopback или private network, а internal application port не меняется вместе с host port.
- [x] Management launcher указывает на стабильную установленную версию installer, а не на первоначальный clone.
- [x] Удаление исходного clone не ломает maintenance launcher.
- [x] Обычный uninstall удаляет только выбранный stack и сохраняет management/recovery tooling.
- [x] Полное удаление installer требует отдельного явного подтверждения.
- [x] Автоматические integration tests покрывают non-root writes, two-project isolation и launcher behavior после uninstall.
- [x] Runbook описывает project identity, filesystem permissions и варианты удаления.

## Verification record

> *This was generated by AI during triage.*

2026-08-15 live lifecycle подтвердил UID `1000`, writes во все persistent Bot
directories, loopback-only backend, два одновременно запущенных project scopes с
разными labels/networks/volumes и отсутствие leftovers. После полного удаления
stack `/usr/local/bin/vpn` и `/opt/bedolaga-installer/current/bot-menu.sh`
остались executable; private lifecycle receipt имеет mode `600`.

## Blocked by

- [Issue 01: Безопасный минимальный профиль установки](./issue-01-secure-minimal-installation-profile.md)
